class CalendarConfig {
  static final DateTime firstDay = DateTime(2025, 10, 1);
  static final DateTime lastDay = DateTime(DateTime.now().year + 3);
  static final DateTime today = DateTime.now();
}
